## Running Docker
To ensure we are all using the same Node version and to avoid dependency errors, please use Docker:

1. Install [Docker Desktop](https://www.docker.com/products/docker-desktop/).
2. Clone the repo: `git clone <repo-url>`
3. Run the command:
   ```bash
   docker-compose up --build
